Version 0.5
* Clean up code (kind-of hacky ATM)
* Benchmark against other utilities
* User interface (?)

Version 0.4
* More customization options
* Stability

Version 0.3
* Write readme
* Allow user to use a dictionary
* Allow user to set number of threads to use

Version 0.2
* Allow selection of character set to use (alpha, alpha numeric, numeric, etc.) [DONE]
* Allow user to set binary to use (default 'unrar')
* Allow user to set binary options to use (default 't -p')

Version 0.1
* Brute Force working